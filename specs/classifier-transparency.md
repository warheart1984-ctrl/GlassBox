# Classifier Transparency

Classifiers expose:

- classifier identity
- decision name
- category
- confidence
- reason
- raw backend detail when available

The runtime receipt includes every classifier decision returned during execution.
